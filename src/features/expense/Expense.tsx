export interface Expense {
  id: number;
  description: string;
  price: number;
}
const Expense = () => {
  return <section className="p-3 border rounded-md w-fit"></section>;
};

export default Expense;
